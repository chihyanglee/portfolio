---
import type { Lang } from "../i18n";

interface Props {
  title: string;
  summary: string;
  stack: string[];
  slug: string;
  lang: Lang;
}

const { title, summary, stack, slug, lang } = Astro.props;
const href = lang === "zh-TW" ? `/zh/projects/${slug}` : `/projects/${slug}`;
---

<li class="group relative grid pb-1 transition-all lg:hover:!opacity-100 lg:group-hover/list:opacity-50">
  <div class="absolute -inset-x-4 -inset-y-4 z-0 hidden rounded-md transition-all motion-reduce:transition-none lg:block lg:group-hover:bg-slate-200/50 lg:group-hover:shadow-[inset_0_1px_0_0_rgba(148,163,184,0.1)] lg:group-hover:drop-shadow-lg dark:lg:group-hover:bg-slate-800/50" />
  <div class="z-10">
    <h3 class="font-medium leading-snug">
      <a
        href={href}
        class="text-slate-800 dark:text-slate-200 hover:text-teal-500 dark:hover:text-teal-300 focus-visible:text-teal-500 inline-flex items-baseline"
      >
        <span>{title}</span>
        <svg class="ml-1 inline-block h-4 w-4 shrink-0 translate-y-px transition-transform group-hover:translate-x-1 group-hover:-translate-y-1" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 19.5l15-15m0 0H8.25m11.25 0v11.25" />
        </svg>
      </a>
    </h3>
    <p class="mt-2 text-sm leading-normal">{summary}</p>
    <ul class="mt-2 flex flex-wrap" aria-label="Technologies used">
      {stack.map((tech) => (
        <li class="mr-1.5 mt-2">
          <span class="flex items-center rounded-full bg-teal-400/10 px-3 py-1 text-xs font-medium leading-5 text-teal-500 dark:text-teal-300">
            {tech}
          </span>
        </li>
      ))}
    </ul>
  </div>
</li>
